'use strict';

/** @type {import('.')} */
import $gOPD from './gOPD';

if ($gOPD) {
	try {
		$gOPD([], 'length');
	} catch (e) {
		// IE 8 has a broken gOPD
		$gOPD = null;
	}
}

export default $gOPD;
